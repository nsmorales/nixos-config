{ pkgs, config, ... }:

{
  # No shared files currently managed here.
  # Add any cross-platform dotfiles below as needed, e.g.:
  #
  # ".config/some-tool/config" = {
  #   text = builtins.readFile ./config/some-tool/config;
  # };
}
